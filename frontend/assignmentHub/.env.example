# Local development
VITE_API_URL=http://localhost:5000/api

# Production (Vercel): use same-origin /api so cookies work on mobile.
# Vercel rewrites /api → your Render service (see vercel.json).
# VITE_API_URL=/api
